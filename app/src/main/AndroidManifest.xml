<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.isen.emelian.mypharma">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />

    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Client.ProfileManager.PartnerPharma"></activity>
        <activity android:name=".Client.PrescriptionManager.ViewerInfo" />
        <activity android:name=".Client.PrescriptionManager.OverviewActivity" />
        <activity android:name=".Chat.NewChatActivityPro" />
        <activity android:name=".Chat.LatestChatActivityPro" />
        <activity android:name=".Chat.ChatAdapterPro" />
        <activity android:name=".Chat.LatestChatActivity" />
        <activity android:name=".Chat.ChatAdapter" />
        <activity android:name=".Chat.NewChatActivity" />
        <activity android:name=".Client.PrescriptionManager.ChoosePicturePrescription" />
        <activity android:name=".Client.PrescriptionManager.HistoryOption" />
        <activity android:name=".Client.PrescriptionManager.SendOption" />
        <activity android:name=".Pro.ProfileManager.VerificationIdentityPro" />
        <activity android:name=".Pro.ProfileManager.ProfilePharmacy" />
        <activity android:name=".Pro.HomeProActivity" />
        <activity android:name=".Pro.ProfileManager.LoginProActivity" />
        <activity android:name=".Client.PrescriptionManager.SendPrescriptionActivity" />
        <activity android:name=".Client.ProfileManager.PhotoChooseActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Client.Maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Client.ProfileManager.valueListenerAdapter" />
        <activity android:name=".Client.Maps.ViewPlace" />
        <activity android:name=".Client.ProfileManager.ProfileActivity" />
        <activity android:name=".Client.HomeActivity" />
        <activity android:name=".DataClass.User" />
        <activity android:name=".Client.ProfileManager.SignupClientActivity" />
        <activity android:name=".Client.ProfileManager.LoginClientActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".NFC.HostCardEmulatorService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>

            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>
    </application>

</manifest>